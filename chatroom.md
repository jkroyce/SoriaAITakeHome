# Agent chatroom

Append-only activity log, shared by every agent across every worktree.
Newest entries at the bottom. Written by `src/chatroom.py`.

| kind | meaning |
|---|---|
| `SPAWN` | the build manager created an agent |
| `DONE` | an agent finished its section |
| `ASK` / `answer` | a question between agents |
| `DISPATCH` | the runtime manager routed a work item |
| `ESCALATE` | low confidence, retried on a stronger model |
| `FLAG` | still uncertain, sent to human review |
| `LEARN` | an agent wrote itself a new skill rule |

---

`18:38:07Z` **builder → all** · note · Wave 0 complete: contract frozen, 50 announcements cached, base commit 0b36ebc
`18:38:07Z` **builder → all** · note · chatroom online — shared across all worktrees via git-common-dir
`18:38:51Z` **builder → all** · note · Wave 1 fan-out: 4 agents, disjoint file ownership, one branch each. Contract src/schemas.py is frozen.
`18:38:51Z` **builder → agent-extract** · SPAWN · created `agent-extract` on branch `agent/extract` — owns `src/agents/extract.py` — prose -> award rows
`18:38:51Z` **builder → agent-resolve** · SPAWN · created `agent-resolve` on branch `agent/resolve` — owns `src/agents/resolve_entity.py` — contractor -> ticker
`18:38:51Z` **builder → agent-store** · SPAWN · created `agent-store` on branch `agent/store` — owns `src/store.py` — DuckDB persistence + exports
`18:38:52Z` **builder → agent-ui** · SPAWN · created `agent-ui` on branch `agent/ui` — owns `app.py` — Streamlit investor UI
`18:38:52Z` **builder → all** · note · constraint issued to all: NO live API calls — build and validate offline, I run the paid pass under a budget cap
`18:39:51Z` **agent-extract → all** · note · starting: read CLAUDE.md + frozen schemas.py. Contract = 22 model-populated award fields + document_notes; 5 deterministic fields I fill myself. One haiku-4-5 call per day-document via CachedLLM.json_call.
`18:40:09Z` **agent-resolve → all** · note · starting: 12 seed tickers loaded from data/universe.csv; building two-tier resolver (deterministic alias -> batched haiku)
`18:40:25Z` **agent-store → all** · note · starting: src/store.py — 7 tables from the frozen contract, zero model calls
`18:40:49Z` **agent-ui → all** · note · starting: app.py — 5 views (change feed / company / review queue / agent activity / provenance). Reads exported Parquet from data/ only; no store.py import, no model calls. Column names derived from schemas.py field lists, never typed by hand.
`18:42:12Z` **builder → all** · note · correction: worktree branches were auto-named `worktree-agent-<id>`, not the `agent/<section>` names announced above. Will rename to agent/extract, agent/resolve, agent/store, agent/ui before they become PRs.
`18:44:26Z` **agent-store → all** · note · found a real one on Windows: COPY ... TO writes a temp file then moves it, and another DuckDB connection holding the old export (e.g. the Streamlit UI reading data/awards.csv) makes that move fail with Access is denied. Adding a bounded retry + an error that names the cause, rather than pretending exports are always writable.
`18:46:31Z` **agent-extract → all** · note · budget note for the paid pass: one haiku-4-5 call per day-document, ~8.7k input tokens on the biggest doc (4586879), 382k input tokens for all 50 docs = ~$0.38 in + output. max_tokens fixed at 32000 (part of the cache key, so it must not vary with doc size). Suggest smoke-testing 4586879 alone first.
`18:46:31Z` **agent-extract → all** · note · decision: skills/extraction.md seeded with 9 rules (R-001..R-009) derived from the real prose - pool splitting, modification vs option_exercise, asterisk marker, header->enum map from a census of all 50 docs, task-order handling, bid-count rules, which dollar figure is amount_usd. Headers U.S. SPECIAL OPERATIONS COMMAND / U.S. TRANSPORTATION COMMAND / DARPA / DHA / DCSA / WHS have no enum value, so they map to OTHER with the literal header kept in extraction_notes.
`18:46:37Z` **agent-store → builder** · DONE · src/store.py committed (6b2629b). init_db from schemas.all_ddl() -> all 7 tables (29/10/12/9/9/16/8 cols); delete-then-insert upserts on the declared PK in one transaction; load_manifest -> 50 announcements 2026-06-22..2026-08-31 with provenance flattened, and loading it 3x leaves 50 rows; export -> 7 parquet + 5 investor-facing csv, re-read verified; JSON drivers/payload round-trip; helpers pending_announcements / awards_for_ticker / recent_changes / unresolved_contractors / review_queue. Zero model calls. Every column name derives from schemas.TABLES.
`18:47:05Z` **agent-extract → builder** · DONE · built src/agents/extract.py + skills/extraction.md (R-001..R-009) + src/agents/__init__.py, committed f1766ce. One claude-haiku-4-5 call per day-document via CachedLLM.json_call with schemas.extraction_schema() passed verbatim; prompt = base instructions + skills file + fetch.body_text() prose; deterministic post-processing owns award_uid/announcement_id/announced_date/extracted_at/extractor_model/llm_cache_key/skills_version plus exact repairs (asterisk->small_business, pool->is_multi_award, enum guards). Manager entry point: extract_announcement(announcement, llm) -> list[dict] with exactly the awards-table columns. 55 offline checks pass with no API key: schema identity, deterministic cache key, NotCachedError on miss, a stub-LLM end-to-end over the seven-company $160M pool and the $180M P00002/$280M modification, and all 50 manifest docs building a prompt. UNTESTED: no live model output seen - extraction quality and confidence calibration await the paid pass.
`18:48:59Z` **builder → all** · FLAG · cross-agent issue found while verifying: extract R-004 keeps the `*` in contractor_raw ("exactly as printed"), and store keys `entities` on that same string. So "Action Manufacturing Co.,*" and "Action Manufacturing Co." are DIFFERENT keys - split entity + duplicate paid resolutions.
`18:48:59Z` **builder → agent-resolve** · answer · resolution (no schema change, schemas.py stays frozen): keep `entities.contractor_raw` as the raw printed name so the join to awards.contractor_raw is exact, but key the resolver's internal cache + model batching on the NORMALIZED name. Duplicate raw variants then cost zero extra model calls and resolve identically. Routing to agent-resolve.
`18:51:43Z` **agent-ui → builder** · DONE · app.py committed (f0d55a4 on worktree-agent-aa31c6b2c838e7209). 5 views: change feed (materiality-ranked, tier-tinted), company (totals + monthly obligated chart), review queue, agent activity (cache-hit rate, tokens, spend, escalations), provenance (source URL / fetched_at / sha256 / llm_cache_key per reasoning step). Reads data/<table>.parquet or .csv directly — no store.py, no llm.py, no anthropic. Verified: streamlit run bound on :8599 with zero data files and served HTTP 200, no traceback; AppTest exercised all 5 views in both empty and demo modes, 0 exceptions; ast.parse clean; 56 schema column refs audited against schemas.py, 0 invented. Expects table-per-file exports named after the TABLES keys.
`18:53:08Z` **builder → all** · note · ALL STOP requested by user. agent-resolve halted mid-work; agent-extract, agent-store, agent-ui already finished and committed. No merges performed. Nothing has spent API budget.
`19:16:48Z` **builder → agent-resolve** · SPAWN · created `agent-resolve` on branch `agent/resolve` — owns `src/agents/resolve_entity.py` — contractor -> ticker (restart, raw-key constraint)
`19:16:57Z` **agent-resolve → all** · note · starting: entity resolver, src/agents/resolve_entity.py
`19:27:35Z` **agent-fixes → all** · note · applying review findings to app.py, store.py, skills/extraction.md
`23:26:57Z` **agent-golden → all** · note · starting: tests/golden/
`23:26:59Z` **agent-materiality → all** · note · starting: src/agents/score_materiality.py
`23:41:05Z` **builder → all** · note · all four Wave 1 branches MERGED to main after review: store, extract, resolve, ui. Reviewer found 4 blocking UI defects (changes table never loaded; enrich() dropped resolved_at; two hardcoded column lists) - all fixed and verified. skills/extraction.md R-004 cited 8 asterisked entries, real count is 14 (15 asterisks less the *Small business legend); corrected NOW because the file is part of the extraction prompt and the cache is keyed on the prompt, so the same edit after the paid pass would force a full re-extraction.
`23:41:06Z` **builder → all** · note · data/entity_map.json warmed from tier-1 alias matches: 38 companies, 11 tickers, 0 model calls, $0.00. The Facts layer is now a committed artifact.
`23:41:06Z` **builder → all** · note · Wave 2 in flight on disjoint files: agent-materiality (score_materiality.py), agent-golden (tests/golden/), agent-manager (src/manager.py). manager unblocks run.py demo/tick/live, which all currently refuse to run. Still NO live API calls; runtime spend $0.00.
`23:43:01Z` **agent-manager → all** · note · starting: src/manager.py
`23:43:10Z` **agent-materiality → all** · note · restart: src/agents/score_materiality.py, studying corpus by script not by reading
`23:52:16Z` **agent-golden → builder** · DONE · tests/golden/ done on agent/golden (b975a0a). 9 hand-verified cases / 53 checks over announcements 4586879 and 4585867, incl. the 7-company $160M pool and the $180M P00002 mod. runner.py scores extraction output, names field/expected/actual, exits 1 on regression and 2 on blocked; one code path (cache or stub). Bad-rule gate proven: 3 plausible-but-wrong candidate rules all REJECTED, plus a no-op control that returns UNPROVEN. pytest tests/ = 63 passed 1 skipped (the 8 contract tests untouched). schemas.py unmodified. cache/llm/ still empty, $0.00 spent -- no fixture is verified against real model output yet.
`23:58:10Z` **agent-materiality → builder** · DONE · score_materiality.py + skills/materiality.md done on agent/materiality (9e73128, merged up to current main). --selftest = 45 PASS / 0 FAIL offline; pytest tests/ = 63 passed 1 skipped. Deterministic pre-filter takes 275/683 corpus awards (40.3%) away from the model for $0.00; remaining 408 batch into 21 Haiku calls, est $0.63 at list, 3x cheaper than one-call-per-award, $0.00 on replay. tier is banded by code from score and the model tier is overridden + confidence-capped on disagreement. NOTE a real contract gap: materiality has no confidence column, so confidence is prompt-only and survives in review_queue.confidence/payload, not in the table. schemas.py untouched; cache/llm/ still empty, $0.00 spent -- nothing is verified against real model output yet.
`23:59:27Z` **builder → all** · note · MERGED agent/golden and agent/materiality to main. Golden verified independently: good stub PASS/exit0, bad-rule stub exit1, gate REJECTs 3 bad rules with specifics, no-op control returns UNPROVEN (so REJECT discriminates), cache mode exits 2 BLOCKED not 0. Runner cache key 4c4d516ad8c5 == what extract.py computes, so it will score real output unchanged. 64 tests pass.
`23:59:27Z` **builder → all** · note · CONTRACT GAP raised by agent-materiality, confirmed: schemas.MATERIALITY_FIELDS has no confidence column, while entities/agent_runs/review_queue all do. Agent did the right thing per CLAUDE.md rule 2 - did NOT edit the frozen contract, made confidence prompt-only, routed it to review_queue.confidence/.payload, documented it. Consequence: a persisted materiality row carries no record of how sure the scorer was. Needs a human decision; not fixed unilaterally.
`23:59:29Z` **agent-manager → builder** · DONE · src/manager.py committed on agent/manager. tick --dry-run/--offline/--live all work; run.py demo+tick unblocked. 37/37 selftest checks pass, $0.00 spent, cache/llm still empty. Agents discovered dynamically - score_materiality will register with no edit. promote-skills refuses while tests/golden/ is absent.
