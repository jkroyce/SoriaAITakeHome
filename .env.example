# Copy to .env and fill in. Required only for --live runs.
# Every model response is cached under cache/llm/, and that cache is committed,
# so the full pipeline replays offline with no key and no spend:
#     make demo
ANTHROPIC_API_KEY=

# Optional overrides
BULK_MODEL=claude-haiku-4-5
JUDGE_MODEL=claude-opus-5
